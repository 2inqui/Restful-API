This is a Restful service to store users that will be provide to a android app.

The connection string is inside of the app/__init__.py file as "app.config['SQLALCHEMY_DATABASE_URI'] = 'mysql://root:@localhost/messages'" change this line to configure with the local environment of mysql.
